# Disable Middle button click

Thinkpad keyboards use the trackpoint+middle-button to scroll.  
As we also open links with middle button scrolling sucks.  
So i let ChatGPT vibe code a cprogram to disable it.  

You can crete a systemjob (init script/systemd-bs-script) to run it on startup
